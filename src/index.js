import React from "react";
import ReactDOM from "react-dom/client"; // Yeh import change kar
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); // Yeh update kar
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
